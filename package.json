{
  "name": "600-essential-words-toeic",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "CI=true npx --yes pnpm@10 --prefix frontend install --config.confirmModulesPurge=false && npx --yes pnpm@10 --prefix frontend run build && rm -rf backend/static_dist backend/data && cp -r frontend/dist backend/static_dist",
    "dev": "cd frontend && pnpm run dev"
  }
}
